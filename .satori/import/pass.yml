settings:
  files: True
  name: Pass playbook

mutation:
- - value: "Hello World"
    mutate: radamsa
    mutate_qty: 10
  - value: "Hello World"
    mutate: zzuf
    mutate_qty: 10

tests:
  assertReturnCode: 0
  positive:
    assertStdoutContains: "Hello World"
    echo:
    - echo Hello World
  
  negative:
    assertStdoutNotContains: "Hello World"
    echo:
    - echo ${{mutation}}

  touch:
  - touch file
