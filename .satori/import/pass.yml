settings:
  files: True
  name: Pass playbook

mutation:
- - value: "Hello World"
    mutate: radamsa
    mutate_qty: 10
  - value: "Hello World"
    mutate: zzuf
    mutate_qty: 10

tests:
  assertReturnCode: 0
  positive:
    assertStdoutEqual: "Hello World"
    echo:
    - echo -n Hello World
  
  negative:
    assertStdoutNotEqual: "Hello World"
    echo:
    - echo -n ${{mutation}}
